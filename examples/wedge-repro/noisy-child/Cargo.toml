[package]
name = "wedge-noisy-child"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
name = "noisy_child"
crate-type = ["cdylib"]

[dependencies]
packr-guest = { version = "0.6.0", features = ["derive"] }

[profile.release]
opt-level = "s"
lto = true
