[package]
name = "shutdown-test-actor"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
packr-guest = { version = "0.8.1", features = ["pic"] }

[profile.release]
opt-level = "s"
lto = true
