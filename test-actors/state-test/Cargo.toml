[package]
name = "state-test-actor"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
pack-guest = { git = "https://github.com/colinrozzi/pack.git", tag = "v0.2.0" }

[profile.release]
opt-level = "s"
lto = true
