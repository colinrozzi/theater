package test:wasi-http@0.2.0;

world wasi-http-test {
    // Import HTTP types and outgoing handler for making requests
    import wasi:http/types@0.2.0;
    import wasi:http/outgoing-handler@0.2.0;

    // Export the theater actor interface for init
    export theater:simple/actor;

    // Export the incoming handler to handle HTTP requests
    export wasi:http/incoming-handler@0.2.0;
}
