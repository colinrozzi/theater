[package]
name = "composite-test-actor"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
packr-guest = "0.4.0"

[profile.release]
opt-level = "s"
lto = true
