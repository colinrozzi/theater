[package]
name = "tcp-echo-actor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pack-guest = { path = "../../../../../pack/crates/pack-guest" }

[profile.release]
opt-level = "s"
lto = true
